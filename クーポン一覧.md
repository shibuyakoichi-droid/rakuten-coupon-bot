# 自動発行クーポン 一覧（レジストリ）

現在クラウド（GitHub Actions）で自動発行しているクーポンの一覧です。
**基本は発行し続ける運用**で、止めたい時はこちらの判断で止めます（イベント時は月により変更）。
タイプが増えたら、この表に1行足していきます。最終更新: 2026-08-13

## 一覧
| # | 種類 | 対象 | 割引 | 条件 | 発行タイミング | 状態 | 設定ファイル / ワークフロー |
|---|---|---|---|---|---|---|---|
| 1 | 店舗全体クーポン（自動再発行） | 店舗全体 | 現在:3000円以上100円OFF ／ イベント後:2個以上10%OFF | 併用不可 | 常時。50枚使い切ったら自動で次の50枚。JST20-24時=15分毎/他=30分毎 | 🟢 ON | `config.cloud.json` / `coupon-bot.yml` |
| 2 | 商品別クーポン（もっちりシルク腹巻） | 管理番号 `mocchi` | 20%OFF | 無条件・併用不可 | 指定日の朝8:00に50枚・その日限り。**発行日=8/13,14,16,17,18** | 🟢 ON（今月の指定日のみ） | `product_coupon_config.json` / `product-coupon.yml` |
| 3 | 新規会員限定クーポン（単発・手動） | 店舗全体 | 20%OFF | **新規購入者のみ**・2個以上・併用不可 | 手動発行（自動化なし）。その日限り50枚。**2026-08-15に発行済** | ⚪ 手動（都度） | `newmember_coupon_config.json` |

## 発行の履歴（実際に出たクーポン）
- 発行するたびに**LINEに「✅ 発行しました（コード・獲得URL）」が届きます**（商品別クーポン）。
- 店舗全体クーポンの発行履歴は `state.json` の `history` に記録されます。

---

## 止め方（こちらの意思で止める）

### ① 店舗全体クーポンを止める
- **一時停止**：`config.cloud.json` の `automation.enabled` を `true` → `false` にしてコミット。次回から発行しなくなる。
- **再開**：`false` → `true` に戻す。
- 割引内容を変える（例：マラソン後に「2個で10%OFF」へ）：`config.cloud.json` の `coupon` を `_switch_back` の値に書き換えてコミット。

### ② 商品別クーポン（もっちり等）を止める / 追加する
- **止める**：`product_coupon_config.json` の `issue_dates` から日付を消す（または全部空に）してコミット。指定日でない日は発行されません。
- **日を増やす**：`issue_dates` に日付（`"2026-08-20"` 形式）を足す。
- **別商品を追加**：将来は商品ごとに設定を分ける形に拡張予定（この表に行を追加）。

### 全部いっぺんに止めたいとき
- GitHubの各ワークフロー（`coupon-bot` / `product-coupon`）を **Disable** にすれば、その種類の自動発行が完全に止まります（再開はEnableで）。
- 迷ったら Claude に「クーポン全部止めて」と言えば対応します。

---

## 単発（手動）クーポンの出し方
- #3のような「その日だけ」のクーポンは自動化せず、**必要な時にClaudeに「〇〇クーポン出して」と言えば発行**します（設定ファイルは残してあるので、日付や割引を変えて再利用できます）。
- 新規会員限定の設定値メモ: 楽天の購入履歴条件 `purchase_history_type` は **0=制限なし / 1=購入履歴なし(新規) / 2=不可**。

## メモ
- クラウド実行なので**PCオフでも発行**されます。
- licenseKey は90日で失効 → 失効時は Secrets（`RAKUTEN_LICENSE_KEY`）の更新が必要（coupon-bot / stock-alert 両リポジトリ）。
- 発行手数料回避のため**1回の発行は必ず50枚以内**に固定しています。
